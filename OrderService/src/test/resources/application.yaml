Spring:
  config:
    import: optional:configserver:http://localhost:9296
  datasource:
    url: jdbc:h2:mem:order
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update

  application:
    name: ORDER-SERVICE

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://dev-01024931.okta.com/oauth2/default
      client:
        registration:
          internal-client:
            provider: okta
            authorizationGrantType: client_credentials
            scope: internal
            client-id: 0oad4zsj9py7aShPg5d7
            client-secret: 1ikv3-9pLKrOMdom19IUy_wxfFE5N7LMwl5Cv5y1_omRyYjg4qvZ71O2dBHL-1yf
        provider:
          okta:
            issuerUri: https://dev-01024931.okta.com/oauth2/default





#eureka:
#  instance:
#    prefer-ip-address: true
#  client:
#    fetch-registry: true
#    register-with-eureka: true
#    service-url:
#      defaultZone: ${EUREKA_SERVER_ADDRESS:http://localhost:8761/eureka}